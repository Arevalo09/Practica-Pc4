export const seleccion = (selector) => document.querySelector(selector);

export const celdas = document.querySelectorAll(".cell"); // celdas | celda dom

// Este evento se ejecuta cuando se hace click en el botón de reiniciar, lo que hay aqui es una funcion flecha, escribe el codigo dentro del bloque como con otra funcion normal doom

